apiVersion: v1
kind: Secret
metadata:
  annotations:
    networking.istio.io/cluster: security
    avp.kubernetes.io/path: sahab2-kv/data/shared/security-dev    
  labels:
    istio/multiCluster: "true"
  name: security-cluster-remote-secret
  namespace: istio-system
stringData:
  security: |
    apiVersion: v1
    kind: Config
    preferences: {}
    clusters:
    - cluster:
        certificate-authority-data: <certificate-authority-data>
        server: <server>
      name: security
    users:
    - name: security
      user:
        token: <sa-token>
    contexts:
    - context:
        cluster: security
        user: security
      name: security
    current-context: security